name: Build unsigned iOS IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    name: Build unsigned IPA
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Cache OpenCV iOS framework
        id: cache-opencv
        uses: actions/cache@v4
        with:
          path: ThirdParty/OpenCV/opencv2.framework
          key: ${{ runner.os }}-opencv-ios-4.5.5-paddle-demo

      - name: Download OpenCV iOS framework
        if: steps.cache-opencv.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          mkdir -p ThirdParty/OpenCV
          curl -L "https://paddlelite-demo.bj.bcebos.com/libs/ios/opencv-4.5.5-ios-framework.tar.gz" -o ThirdParty/OpenCV/opencv-ios-framework.tar.gz
          tar -xzf ThirdParty/OpenCV/opencv-ios-framework.tar.gz -C ThirdParty/OpenCV
          rm -f ThirdParty/OpenCV/opencv-ios-framework.tar.gz
          find ThirdParty/OpenCV -name '._*' -delete

      - name: Build app without code signing
        run: |
          set -euo pipefail
          xcodebuild \
            -project Lectigo.xcodeproj \
            -scheme Lectigo \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM="" \
            build

      - name: Package unsigned IPA
        run: |
          set -euo pipefail
          APP_PATH="$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -print -quit)"
          if [ -z "$APP_PATH" ]; then
            echo "No .app found in Release-iphoneos output" >&2
            exit 1
          fi
          rm -rf Payload Lectigo-unsigned.ipa
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/Lectigo.app
          /usr/bin/zip -qry Lectigo-unsigned.ipa Payload

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: Lectigo-unsigned-ipa
          path: Lectigo-unsigned.ipa
          if-no-files-found: error
